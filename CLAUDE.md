# Claude Compatibility

This file is a compatibility shim.

Canonical project instructions live in `AGENTS.md`.

If Claude-style tooling is used in this vault:
- Read `AGENTS.md` first and treat it as the source of truth.
- Rebuild your working framework from `AGENTS.md`, `_index.md`, and the current vault state instead of relying on stale duplicate guidance here.
- Follow the same navigation pattern as other agents: start at `_index.md`, open one relevant MOC, then drill into dashboards or reference notes as needed.

If `CLAUDE.md` and `AGENTS.md` ever conflict, `AGENTS.md` wins.
