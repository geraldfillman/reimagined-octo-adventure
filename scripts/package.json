{
  "name": "my-data-scripts",
  "version": "1.0.0",
  "description": "Data pull and signal evaluation scripts for My_Data Obsidian vault",
  "type": "module",
  "scripts": {
    "pull": "node run.mjs",
    "test": "node --test 'tests/**/*.test.mjs'",
    "test:coverage": "node --test --experimental-test-coverage 'tests/**/*.test.mjs'"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "neo4j-driver": "^6.0.1"
  }
}
